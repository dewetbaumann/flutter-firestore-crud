import 'package:flutter/material.dart';

class DeleteFirestore extends StatelessWidget {
  const DeleteFirestore({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create data')),
      body: null,
    );
  }
}
